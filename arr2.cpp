//Different way to create array
//initalization
//User Input

int arr[5]={1,2,3,4,5}; //Initalization first way.
int a[100]={1,2,3};//second way of initalization.
int a[100]={0};//all container aligned 0; true only for 0 case;
int a[100]={1}; //only oth index will have value 1 and rest 0;
int a[]={1,3,4,5,6} //Bydefault the size will be 5;

// we not skip the size while initalisizing like a[100]; we can do a[] only when declearing the elements of particular arr like a[]={1,2,3,4};

//brute force, cout<<a[1]<<a[2] and soon;

//using for loop to cout the a[i];


int n;
cin>>n;

int a[n];

for(int 1=0;i<n;i++){

	cin>>a[i];
}

